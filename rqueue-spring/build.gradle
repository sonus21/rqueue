ext.projectDescription = 'Asynchronous and delayed task executor for spring framework'
ext.name = 'Rqueue Spring Java'
buildscript {
    apply from: "${rootDir}/gradle/code-signing.gradle"
    apply from: "${rootDir}/gradle/packaging.gradle"
}

dependencies {
    compile project(":rqueue-core")
    testCompile project(":rqueue-common-test")
}